export type Student = {
    id: string
    username: string
    enrollmentId: string
    registerDate: String
    role: string
    subscription: string
    email: string
  }
  export type Instructor = {
    id: string
    profile: string
    username: string
    registerDate: String
    role: string
    email: string
  }